# zaif_api_trader
a command line tool using zaif api to trade

Usage:

    python order.py -b -a [coin] [price] [amount] [F|FF]

                    -c    [trade_id]
                
                    -l
